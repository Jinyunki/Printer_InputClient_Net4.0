<UserControl x:Class="Printer_InputClient_Net4._0.View.PositionDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Printer_InputClient_Net4._0.View"
             mc:Ignorable="d" 
             DataContext="{Binding Main, Source={StaticResource Locator}}"
             d:DesignHeight="900" d:DesignWidth="1600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="22*"/>
            <RowDefinition Height="403*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">

            <TextBlock Text="{Binding WorkSheetName}" Background="Black" Foreground="White" FontSize="25" Margin="3"/>

        </Grid>

        <Grid Grid.Row="1" Grid.RowSpan="2">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.5*"/>
                        <ColumnDefinition Width="0.5*"/>
                        <ColumnDefinition Width="8*"/>
                    </Grid.ColumnDefinitions>

                    <ItemsControl ItemsSource="{Binding PositionCategorise}" Grid.Column="0" Background="Black" Margin="3">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <!-- 가로 방향으로 배치하기 위해 StackPanel 사용 -->
                                <StackPanel Orientation="Vertical"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" 
                                           Height="30" TextAlignment="Center" 
                                           FontSize="15" Foreground="White" Margin="3"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>


                    <ItemsControl ItemsSource="{Binding PositionData}" Grid.Column="1" Background="Black" Margin="3">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <!-- 가로 방향으로 배치하기 위해 StackPanel 사용 -->
                                <StackPanel Orientation="Vertical"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Path=.}" VerticalAlignment="Center" Height="30"
                                         Width="100" TextAlignment="Center" Background="Black" Opacity="0.8"
                                         FontSize="15" Foreground="White" Margin="3"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Grid.Column="2" Text="{Binding InputPrinterCommand}" Background="White"/>
                </Grid>
            </ScrollViewer>
        </Grid>

    </Grid>
</UserControl>
